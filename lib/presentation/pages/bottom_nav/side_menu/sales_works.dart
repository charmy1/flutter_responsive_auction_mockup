

import 'package:flutter/material.dart';

class SalesWork extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("SalesWork"),
      ),
    );
  }
}
