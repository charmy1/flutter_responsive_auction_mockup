import 'package:flutter/material.dart';

class MyAuctionOffers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("My Auction Offers "),),
    );
  }
}

class MyAuctionOffers1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("--"),),
    );
  }
}