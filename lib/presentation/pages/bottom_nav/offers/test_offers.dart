import 'package:flutter/material.dart';

class TestOffers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("CHid test offers"),
      ),
    );
  }
}
